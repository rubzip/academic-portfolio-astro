---
import { SITE, SOCIALS, SOCIAL_ICONS } from "../../config";
import { getCollection } from "astro:content";
import Icon from "../ui/Icon.astro";

const activeSocials = SOCIALS.filter((s) => s.isActive);
const bioCollection = await getCollection("bio");
const bio = bioCollection[0]?.data;
---

<aside class="sidebar max-md:flex-row max-md:flex-wrap max-md:gap-4">
    <div class="flex flex-col items-center text-center max-md:flex-row max-md:text-left max-md:gap-4 max-md:items-center">
        {bio?.avatar && (
            <img
                src={`/${bio.avatar}`}
                alt={bio.name}
                class="sidebar-avatar"
            />
        )}
        <div class="flex flex-col items-center max-md:items-start">
            {bio?.name && <h1 class="sidebar-name">{bio.name}</h1>}
            {bio?.institution && (
                <p class="sidebar-info">
                    <Icon name="Institution" size={16} />
                    {bio.institution}
                </p>
            )}
            <div class="flex gap-1 flex-wrap items-center justify-center max-md:justify-start mt-4">
                {activeSocials.map((social) => (
                    <a
                        href={social.href}
                        aria-label={social.linkTitle}
                        title={social.linkTitle}
                        target="_blank"
                        rel="noreferrer"
                        class="social-link"
                    >
                        <Icon name={social.name} size={20} />
                    </a>
                ))}
            </div>
        </div>
    </div>

    {bio?.shortBio && (
        <p class="sidebar-bio max-md:w-full max-md:order-3">{bio.shortBio}</p>
    )}
</aside>