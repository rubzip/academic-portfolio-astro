---
import { getCollection } from "astro:content";
import Layout from "../layouts/Layout.astro";

const talks = await getCollection("talks");
const sortedTalks = talks.sort(
    (a, b) => b.data.date.getTime() - a.data.date.getTime(),
);
---

<Layout title="Talks">
    <header>
        <h1>Talks & Presentations</h1>
        <p class="subtitle">
            Presentations given at various institutions and conferences.
        </p>
    </header>

    <section class="talks-list">
        {
            sortedTalks.map((talk) => (
                <article class="talk-item">
                    <div class="date">
                        {talk.data.date.toLocaleDateString("en-US", {
                            year: "numeric",
                            month: "long",
                            day: "numeric",
                        })}
                    </div>
                    <a href={`/talks/${talk.id}`} class="title-link">
                        <h3>{talk.data.title}</h3>
                    </a>
                    <p class="event">
                        <strong>{talk.data.event}</strong>
                    </p>
                    <p class="location">{talk.data.location}</p>
                    <p class="description">{talk.data.description || ""}</p>
                    <div class="links">
                        {talk.data.slides && (
                            <a href={talk.data.slides} target="_blank">
                                Slides
                            </a>
                        )}
                        {talk.data.video && (
                            <a href={talk.data.video} target="_blank">
                                Video
                            </a>
                        )}
                    </div>
                </article>
            ))
        }
    </section>
</Layout>

<style>
    h1 {
        font-size: 2.5rem;
        font-weight: 800;
        margin: 0;
    }
    .subtitle {
        font-size: 1.1rem;
        color: var(--text-muted);
        margin-top: 0.5rem;
        margin-bottom: 3rem;
    }
    .talks-list {
        display: flex;
        flex-direction: column;
        gap: 3rem;
    }
    .talk-item {
        padding-bottom: 2rem;
        border-bottom: 1px solid var(--border);
    }
    .talk-item:last-child {
        border-bottom: none;
    }
    .date {
        font-size: 0.9rem;
        color: var(--text-muted);
        margin-bottom: 0.5rem;
    }
    .title-link {
        text-decoration: none;
        color: inherit;
    }
    .title-link:hover h3 {
        text-decoration: underline;
    }
    h3 {
        font-size: 1.5rem;
        margin: 0.5rem 0;
        line-height: 1.3;
    }
    .event {
        margin-bottom: 0.25rem;
    }
    .location {
        color: var(--text-muted);
        margin-bottom: 1rem;
    }
    .description {
        margin-top: 1rem;
        line-height: 1.6;
    }
    .links {
        margin-top: 1.5rem;
        display: flex;
        gap: 1.5rem;
    }
    .links a {
        text-decoration: underline;
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--foreground);
    }
    .links a:hover {
        opacity: 0.7;
    }
</style>
